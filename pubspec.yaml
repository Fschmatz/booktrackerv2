name: booktrackerv2
description: Tracker para livros, criando nova versão para testar erro do SDK
publish_to: 'none'

version: 5.6.2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  path_provider:
  shared_preferences:
  provider:
  image_picker:
  flutter_native_image:
  url_launcher:
  easy_dynamic_theme:
  animations:
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints:

flutter:
  uses-material-design: true
  assets:
    - assets/
